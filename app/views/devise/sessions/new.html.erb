<div class="row">
  <div class="col m6 offset-m3 center">

    <h2>Log in</h2>

    <%= link_to "Login with Google", user_omniauth_authorize_path(:google_oauth2), class: "btn red darken-1" %>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <%= render partial: "shared/devisesmes" %>

      <div class="input-field">
        <%= f.email_field :email, :id => "email", :class => "validate", :placeholder => "Email" %>
      </div>

      <div class="input-field">
        <%= f.password_field :password, :id => "password", :class => "validate", :placeholder => "Password", autocomplete: "off" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end -%>
      <br />
      <button class="btn waves-effet waves-light" type="submit" name="action">Login</button>
      <%= link_to "Forgot password", new_user_password_path, :class => "btn waves-effect waves-light" %>
    <% end %>

 </div>
</div>


